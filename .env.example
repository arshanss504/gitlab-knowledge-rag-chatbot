# Required
GEMINI_API_KEY=

# Optional (defaults work for local Docker)
REDIS_URL=redis://localhost:6379
CHROMA_PERSIST_DIR=./chroma_db
LOG_LEVEL=INFO

# Comma-separated browser origins for CORS (e.g. https://myapp.vercel.app). Leave empty to allow all.
# CORS_ORIGINS=
